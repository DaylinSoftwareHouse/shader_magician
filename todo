[x] Fix false shared struct deps when common names involved (VertexInput, VertexOutput, etc)
[ ] Fix or add error for groups with items the same name as the function param
[x] Can we fix build failing cause rust-analyzer to fail?

# Shaders
[x] Texture processing
[x] Handle @vertex and @fragment with standardized names
[?] May need better binding and group assignment algo
[x] Don't require shaders provide groups in the same order

# Pipelines
[x] Allow for seperate vertex and fragment shaders with custom main functions
[x] Create solution for texture_bind_group_layout
[ ] Fix bind group entries (maybe switch to trait style system instead of dependence on macros)
[ ] Load pipeline parameters from shader macros