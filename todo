[x] Create examples and test cases for existing functionality to produce combined final shaders
[x] Introduce @public notations
[x] Allow for production of split final fragment and vertex shaders
[x] Shader modifiers
[x] Finish @default support so that main functions do not need to fill everything
[x] @mut attribute for mutable parameters in functions
[ ] Bring back # replacements (look for #[allow] markers that hide the warnings for the unused left-overs)
[ ] Better conditional compilation support using #ifdef, #else, and #endif