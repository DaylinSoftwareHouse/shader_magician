# Shader Magician
A tool for composing final GPU-ready shaders from a shaders from various shader source files and
libraries.  This library also provides a `Syn` like library for interacting with shader source in a
code driven method.